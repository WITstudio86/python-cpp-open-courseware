# 第 44 节 · 项目 13：打字速度测试（上）

## 学习目标
- 学会随机选择范文并显示在 Label 上 🎯
- 学会用 Text 控件做打字输入区 ⌨️
- 学会实时逐字对比高亮——打对变绿、打错变红 🟢🔴

## 本节课成品
一个能实时对比的打字练习器——你一边打字，程序一边告诉你哪里对、哪里错！

---

## 知识点讲解

### 1. 随机选择范文

准备一个范文列表，用 `random.choice()` 随机选一篇：

```python
import random

samples = [
    "今天天气真好，阳光洒在操场上，同学们在跑步。",
    "Python 是一门非常有趣的编程语言，简单又强大。",
    "千里之行，始于足下。学习编程也是一步一步来的。",
    "春天来了，花儿开了，小鸟在枝头唱着欢快的歌。",
    "努力不一定会成功，但不努力一定不会成功。",
]

current_text = random.choice(samples)
text_label.config(text=current_text)
```

### 2. 显示范文和打字区

范文用 Label 显示（较大字体，方便阅读），打字区用 Text 控件：

```python
# 范文显示区域
text_label = tk.Label(root, text=current_text, font=("微软雅黑", 18),
                      bg="white", fg="#333", wraplength=600,
                      justify="left", padx=20, pady=20)
text_label.pack(pady=20)

# 打字输入区域
input_text = tk.Text(root, font=("微软雅黑", 18), height=4, width=50,
                     wrap="word", bg="#fafafa")
input_text.pack(pady=10)
```

> `wraplength` 让文字自动换行，避免超出窗口。

### 3. 实时逐字对比高亮 ← 核心功能！

这是本节课最酷的部分！用 Text 控件的 `tag_config` 给文字上色：

```python
# 配置两个 tag：正确 = 绿色，错误 = 红色
input_text.tag_config("correct", foreground="green")
input_text.tag_config("wrong", foreground="red", background="#ffeeee")

def check_typing(event=None):
    """每次按键后，对比用户输入和范文，高亮显示"""
    user_text = input_text.get("1.0", tk.END).rstrip("\n")
    
    # 先清除之前的高亮
    input_text.tag_remove("correct", "1.0", tk.END)
    input_text.tag_remove("wrong", "1.0", tk.END)
    
    # 逐字对比
    for i, char in enumerate(user_text):
        pos = f"1.{i}"
        if i < len(current_text) and char == current_text[i]:
            input_text.tag_add("correct", pos)
        else:
            input_text.tag_add("wrong", pos)

# 绑定按键事件
input_text.bind("<KeyRelease>", check_typing)
```

**工作原理**：
1. 用户每按一个键，`KeyRelease` 事件触发
2. 获取 Text 中用户打的所有文字
3. 清除之前的高亮标记
4. 逐个字符对比：和范文一致 → 绿色；不一致 → 红色

---

## 完整代码

```python
import tkinter as tk
import random

# ========================================
# 第 44 节：打字速度测试（上）—— 实时逐字对比
# ========================================

# 范文库
SAMPLES = [
    "今天天气真好，阳光洒在操场上，同学们在快乐地跑步。",
    "Python 是一门非常有趣的编程语言，简单又强大。",
    "千里之行，始于足下。学习编程也是一步一步来的。",
    "春天来了，花儿开了，小鸟在枝头唱着欢快的歌。",
    "努力不一定会成功，但不努力一定不会成功。",
    "书籍是人类进步的阶梯，多读书，读好书。",
]

root = tk.Tk()
root.title("打字速度测试 V1.0")
root.geometry("700x450")
root.configure(bg="#f5f5f5")

# 随机选择一篇范文
current_text = random.choice(SAMPLES)

# ---------- 标题 ----------
title_label = tk.Label(root, text="⌨️ 打字速度测试",
                       font=("微软雅黑", 16, "bold"), bg="#f5f5f5", fg="#333")
title_label.pack(pady=(15, 5))

# ---------- 范文显示区域 ----------
sample_frame = tk.Frame(root, bg="white", relief=tk.RIDGE, bd=2)
sample_frame.pack(pady=10, padx=30, fill=tk.X)

tk.Label(sample_frame, text="📝 请打以下文字：", font=("微软雅黑", 11),
         bg="white", fg="#888", anchor="w").pack(anchor="w", padx=15, pady=(10, 0))

text_label = tk.Label(sample_frame, text=current_text,
                       font=("微软雅黑", 16), bg="white", fg="#333",
                       wraplength=620, justify="left", padx=15, pady=10)
text_label.pack()

# ---------- 打字输入区域 ----------
tk.Label(root, text="👇 在下面输入：", font=("微软雅黑", 11),
         bg="#f5f5f5", fg="#888").pack(anchor="w", padx=33, pady=(15, 5))

input_text = tk.Text(root, font=("微软雅黑", 16), height=4, width=52,
                     wrap="word", bg="white", relief=tk.SUNKEN, bd=1)
input_text.pack(pady=0, padx=30)

# 配置高亮 tag
input_text.tag_config("correct", foreground="#2e7d32")       # 正确 = 绿色
input_text.tag_config("wrong", foreground="#c62828",
                      background="#ffebee")                   # 错误 = 红色 + 浅红背景


def check_typing(event=None):
    """实时逐字对比：正确的字变绿，错误的字变红"""
    user_text = input_text.get("1.0", tk.END).rstrip("\n")

    # 清除之前的高亮
    input_text.tag_remove("correct", "1.0", tk.END)
    input_text.tag_remove("wrong", "1.0", tk.END)

    # 逐字对比
    for i, char in enumerate(user_text):
        pos = f"1.{i}"
        if i < len(current_text) and char == current_text[i]:
            input_text.tag_add("correct", pos)
        else:
            input_text.tag_add("wrong", pos)


# 绑定按键事件：每打一个字就触发对比
input_text.bind("<KeyRelease>", check_typing)


# ---------- 换一篇按钮 ----------
def change_sample():
    global current_text
    current_text = random.choice(SAMPLES)
    text_label.config(text=current_text)
    input_text.delete("1.0", tk.END)


tk.Button(root, text="🔄 换一篇", command=change_sample,
          bg="#e3f2fd", font=("微软雅黑", 11), width=12).pack(pady=15)

# 统计提示
info_label = tk.Label(root, text="💡 打对的字会变绿色，打错的字会变红色哦~",
                      font=("微软雅黑", 9), bg="#f5f5f5", fg="#888")
info_label.pack(pady=(0, 10))

root.mainloop()
```

---

## 动手试试

1. **打一遍范文**，观察绿色和红色的变化
2. **故意打错几个字**，看看红色的效果
3. **点"换一篇"**，试试不同长度的范文
4. **挑战**：在范文库中增加你自己的文章，比如一段课文或歌词

---

## 小贴士
- Text 控件的索引格式是 `"行.列"`，比如 `"1.0"` 是第 1 行第 0 个字符（开头）
- `tag_config` 可以设置任意样式：颜色、背景、字体等
- `tag_add("tag_name", "1.0", "1.5")` 给第 1 行第 0-4 个字符加上标记
- `"1.0"` 到 `"1.end"` 表示整行，`tk.END` 表示文本末尾
- `random.choice(列表)` 从列表中随机选一个元素
- 如果用户打的字比范文长，多余的字都算错（变红）
